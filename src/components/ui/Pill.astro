---
interface Props { label: string; href: string }
const { label, href } = Astro.props as Props;
---
<a href={href} class="inline-block rounded-full border border-gray-200 px-3 py-1 text-xs hover:bg-gray-50">{label}</a>
