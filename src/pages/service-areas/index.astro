---
import MarketingPage from "../../layouts/MarketingPage.astro";
import PageHeader from "../../components/ui/PageHeader.astro";
import Card from "../../components/ui/Card.astro";
import areas from "../../data/service-areas.json";
import { slugify, canonical } from "../../lib/slug";

const title = "Service Areas — San Diego County | Postal Systems";
const description = "USPS-approved mailbox installation across San Diego County. Find your city below.";
const url = canonical("/service-areas");
---

<MarketingPage
  title={title}
  description={description}
  canonicalUrl={url}
  breadcrumbs={[{href:"/",label:"Home"},{label:"Service Areas"}]}
>
  <PageHeader eyebrow="Coverage" title="Service Areas" subtitle="USPS-approved installation across San Diego County." />

  <section class="container py-12">
    <div class="flex flex-wrap gap-2">
      {areas.map(a => (
        <a href={`/service-areas/${slugify(a)}`}
           class="inline-flex items-center rounded-full bg-slate-100 px-3 py-1 text-sm text-slate-700 ring-1 ring-slate-200 hover:bg-slate-200">
          {a}
        </a>
      ))}
    </div>
  </section>
</MarketingPage>
